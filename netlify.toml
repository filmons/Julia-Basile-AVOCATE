# Configuration Netlify pour Julia Basile - Avocate
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  publish = "."
  command = "echo 'Site statique - pas de build nécessaire'"

# Redirection après soumission du formulaire
[[redirects]]
  from = "/contact-success"
  to = "/#contact"
  status = 200
  force = false

# Headers de sécurité
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Cache pour les assets statiques
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Configuration des formulaires
[forms]
  # Activer les notifications par email (à configurer dans le dashboard)
  # Email par défaut pour les notifications

# Pages d'erreur personnalisées (optionnel - à créer plus tard)
# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404
